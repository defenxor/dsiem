apiVersion: v1
kind: Service
metadata:
  name: dsiem-backend-trace-1
  labels:
    app: dsiem-backend-trace-1
spec:
  selector:
    app: dsiem-backend
    statefulset.kubernetes.io/pod-name: dsiem-backend-1
  ports:
  - name: http
    port: 8087
    targetPort: 8087
    protocol: TCP
